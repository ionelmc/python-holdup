version: "3.4"

services:
  pg:
    image: postgres:9.4
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_DB: app
  test:
    build:
      context: ..
      dockerfile: tests/Dockerfile
    depends_on:
      - pg
    volumes:
      - $PWD:/src
